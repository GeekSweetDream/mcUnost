<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:statusBarColor">@color/md_orange_500</item>
        <item name="colorAccent">@color/colorBlueToolbar</item>
    </style>
    <style name="AppTheme.NoActionBar.FullScreen" parent="Theme.AppCompat.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Splash screen theme. -->
    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splashscreen2</item>
    </style>

    <style name="AlertDialogCustom" parent="@android:style/Theme.Dialog">
        <item name="android:textColor">@color/md_blue_800</item>
    </style>

    <style name="MySwitch" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/md_orange_400</item>
        <item name="colorSwitchThumbNormal">@color/colorBlueToolbar</item>
        <item name="android:colorForeground">@color/colorBlueToolbar</item>
    </style>

    <style name="MyTextInputLayout" parent="Widget.Design.TextInputLayout">
        <item name="hintTextAppearance">@style/AppTheme.TextFloatLabelAppearance</item>
        <item name="errorTextAppearance">@style/AppTheme.TextErrorAppearance</item>
        <item name="android:textColorHint">@color/md_white_1000</item>
        <item name="android:colorControlNormal">@color/md_white_1000</item>
        <item name="android:colorControlActivated">@color/md_white_1000</item>
    </style>

    <style name="MyColorGreyHint" parent="Widget.Design.TextInputLayout">
        <item name="android:colorControlActivated">@color/md_blue_600</item>
        <item name="android:textColorHint">@color/md_blue_grey_400</item>
        <item name="android:colorControlNormal">@color/md_grey_400</item>
    </style>

    <style name="AppTheme.TextFloatLabelAppearance" parent="TextAppearance.Design.Hint">
        <!-- Floating label appearance here -->
        <item name="android:textColor">@color/colorBlueToolbar</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="AppTheme.TextErrorAppearance" parent="TextAppearance.Design.Error">
        <!-- Error message appearance here -->
        <item name="android:textColor">#ff0000</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="MyCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/md_blue_600</item>
        <item name="colorControlActivated">@color/md_blue_600</item>
    </style>

</resources>
